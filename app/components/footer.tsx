import Links from "../components/links";

const Footer = () => {
  return (
    <footer className="relative flex flex-col items-center justify-between gap-4 mx-auto sm:flex-row w-max">
      <Links />
    </footer>
  );
};

export default Footer;